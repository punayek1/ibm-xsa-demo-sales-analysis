ID: SalesAnalysis
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: coredb
   type: hdb
   path: coredb
   requires:
    - name: hdi_coredb

resources:
 - name: hdi_coredb
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
