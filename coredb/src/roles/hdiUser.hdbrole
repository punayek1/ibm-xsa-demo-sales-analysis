{ 
  "role":{ 
    "name":"SalesAnalysis.coredb.roles::masterdataAccess",
    
    "schema_privileges":[
    { 
        "privileges":[ "SELECT", "SELECT METADATA", "SELECT METADATA", "SELECT CDS METADATA", "EXECUTE" ]
    }],
    "object_privileges":[
    {
        "name": "SalesAnalysis.coredb.structures::MasterData.Customers",
        "type": "TABLE", 
        "privileges": [ "SELECT" ]
    }]
  } 
}