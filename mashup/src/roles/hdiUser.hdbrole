{ 
  "role":{ 
    "name":"SalesAnalysis.mashup.roles::mashupAccess",
    
    "schema_privileges":[
    { 
        "privileges":[ "SELECT", "SELECT METADATA", "SELECT METADATA", "SELECT CDS METADATA", "EXECUTE" ]
    }],
    "object_privileges":[
    {
        "name": "SalesAnalysis.mashup.views::CV_SALES_STATS",
        "type": "VIEW", 
        "privileges": [ "SELECT" ]
    }]
  } 
}